---
import Section from "./Section.astro"
---

<Section
  title="Cafetería"
  subtitle="En nuestra cafetería ofrecemos bebidas y postres que están en tendencia o son virales en redes sociales, como, así también brindamos las bebidas tradicionales"
  color="black"
  id="cafeteria"
>
  <div slot="footer" class="gap-x-6 flex mx-auto">
    <a
      class="border-[3px] border-gray-600 bg-gray-600 text-sm rounded font-medium text-white px-12 py-2 inline-block transition-colors hover:border-gray-500 hover:bg-gray-500"
      href="#ofertas"
    >
      Volver</a
    >
    <a
      class="bg-white text-sm rounded font-medium px-12 py-2 transition-colors hover:bg-black hover:text-white inline-flex justify-center items-center text-black"
      href="https://organicahuancavelica.github.io/"
    >
      Comprar ahora</a
    >
  </div>

  <img
    slot="background"
    alt="Model X"
    class="h-full w-full object-cover object-center"
    src="cafe.jpg"
  />
</Section>

<script>
  function isEdge() {
    if (
      navigator.userAgent.indexOf("Edge") > -1 ||
      navigator.userAgent.indexOf("Edg") > -1
    ) {
      return true
    } else {
      return false
    }
  }

  let imageElement = document.querySelector(
    "#bg-image-modelx"
  ) as HTMLImageElement

  if (isEdge()) {
    imageElement.src = "model-x.webp"
  } else {
    imageElement.src = "model-x.avif"
  }
</script>
